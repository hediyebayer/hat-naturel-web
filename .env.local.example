# hatoperasyon backend (rezervasyon API'si). Boş bırakılırsa fallback mock fiyat gösterilir.
# Örn: https://hatoperasyon.com (production), http://localhost:3000 (local)
HATOPERASYON_API_URL=
HATOPERASYON_PUBLIC_API_KEY=

# MASTER SWITCH (kill-switch): Ödeme aşamasını tamamen kapat.
# Hatoperasyon rezervasyon KAYDI endpoint'i hazır olana kadar true tut.
# true  → kart/ödeme YOK, 'İletişime Geç', /odeme sayfası redirect.
# false → ödeme akışı açık.
NEXT_PUBLIC_PAYMENTS_DISABLED=true

# GEÇİCİ: Hatoperasyon bağlanana kadar fallback fiyatlarla ödeme akışını aç.
# (PAYMENTS_DISABLED=true iken etkisiz.)
NEXT_PUBLIC_FORCE_PAYMENT_ON_FALLBACK=false

# Email gönderimi — transport zinciri: RESEND_API_KEY varsa Resend, yoksa SMTP (Gmail app
# password), ikisi de yoksa mock (console.log). İkisini birlikte doldurmayın; Resend önceliklidir.
RESEND_API_KEY=
# Gmail SMTP (app password: myaccount.google.com/apppasswords — 2FA açık olmalı).
# SMTP_PASS'te boşluklar önemli değil; düz 16 hane de olur.
SMTP_USER=hatnaturel@gmail.com
SMTP_PASS=
CONTACT_EMAIL_TO=info@hatnaturel.com.tr

# Site URL (canonical/SEO)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Google Tag Manager (reklamçıdan gelen kurulum). Boş bırakılırsa GTM render edilmez.
NEXT_PUBLIC_GTM_ID=GTM-TBGHC9BP

# Ödeme provider — gerçek tahsilatı belirler. Banka onayı + kimlik bilgileri gelene kadar 'mock'.
PAYMENT_PROVIDER=mock          # mock | vakifbank
# Ödeme GÖRÜNÜM modu (sadece UI). 'live' = DEMO filigranı gizli (banka denetimi/başvuru için).
# 'demo' = DEMO uyarıları görünür (geliştirme). Gerçek para PAYMENT_PROVIDER'a bağlı, buna değil.
NEXT_PUBLIC_PAYMENT_MODE=demo  # demo | live
NEXT_PUBLIC_DEPOSIT_RATIO=0.3
PAYMENT_EMAIL_TO=rezervasyon@hatnaturel.com.tr

# Geçici kart kasası şifreleme anahtarı.
# ÖNERİLEN: `openssl rand -base64 32` ile üretin. 32-byte base64 veya 64-char hex olmalı.
# Tanımlı değilse sistem geriye uyumluluk için legacy merchant credential türetmesine düşer.
CARD_ENCRYPTION_KEY=

# Geçici in-memory store risk onayı. true yapınca runtime uyarısı susar ama risk bitmez.
PAYMENT_STORE_ACKNOWLEDGE_IN_MEMORY_RISK=false

# VAKIFBANK (TODO — banka onayından sonra doldurulacak)
VAKIFBANK_MERCHANT_ID=
VAKIFBANK_TERMINAL_ID=
VAKIFBANK_STORE_KEY=
# Güvenli varsayılan: callback HashData zorunlu. Sadece geçici rollback için false yap.
VAKIFBANK_CALLBACK_HASH_REQUIRED=true
# Gerekirse banka dokümanındaki alan sırasını CSV olarak override et.
# Örn: MerchantId,VerifyEnrollmentRequestId,PurchaseAmount,Currency,Status,Eci,Cavv
VAKIFBANK_CALLBACK_HASH_FIELDS=
VAKIFBANK_MPI_URL=https://onlineodemetest.vakifbank.com.tr:4443/MPIAPI/MPI_Enrollment.aspx
VAKIFBANK_3DSECURE_URL=https://3dsecure.vakifbank.com.tr:4443/MPIAPI/MPI_Enrollment.aspx
VAKIFBANK_ENV=test
